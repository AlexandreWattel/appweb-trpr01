import { ref } from "vue";
export const nom = ref<HTMLInputElement | null>(null);
export const description = ref<HTMLTextAreaElement | null>(null);
export const price = ref<HTMLInputElement | null>(null);
export const stock = ref<HTMLInputElement | null>(null);
